<Window x:Class="WpfApp6.View.Start"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="Start" >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel  Grid.Row="0" Orientation="Vertical" Margin="0 0 0 0" >
            <Button Content="SHow all persons" Margin="0 0 0 0" Click="Button_Click" />
            <Button Content="Create a new person" Margin="0 2 0 0" Click="Button_Click_1" />
        </StackPanel>
        <Frame Grid.Row="2" x:Name="Main" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
